autoOpen: false
apps:
  - port: 4000
    run: sudo service postgresql start && uv run ./main.py 
    root: /studio/backend
    name: backend
    description: LiteLLM Backend
    autoOpen: true
  - port: 8080
    run: uv run main.py
    root: /studio/frontend
    name: frontend
    description: OpenWebui Frontend
    autoOpen: true